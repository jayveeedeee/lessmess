# web/templates

<!-- tasktracker:begin -->
- Purpose: Go `html/template` sources for the lessmess web UI; each file defines named templates rather than serving standalone pages.
- `layout.html` defines the base `layout` shell and renders the `content` template; page templates (`index.html`, `board.html`) only define `content`.
- `partials.html` holds shared fragments (`boardFragment`, `taskDetail`, `planDetail`, `ledgerDetail`, `banner`) pulled in via `{{template ...}}`, often for htmx-swapped partial responses.
- Conventions: server data lives under `.Data`, htmx attributes (`hx-get`, `hx-post`, `hx-target`) drive interactivity, and custom funcs like `statusClass`, `base`, and `markdown` are registered by the Go renderer.
- Gotcha: template names must stay globally unique across files since they are parsed into one set; renaming a `define` can break handlers that reference it.
- (2026-09-12-2) `index.html` now exposes only the opencode-driven "New change session" form (`hx-post="/changes/session"`); the plain "New change" form was removed from the UI, but the `POST /changes` endpoint remains for API clients, so do not resurrect that form without a decision.
- (2026-09-12-2) When verifying a template edit in the running UI, a stale server process holding the port can serve old HTML and make the change look unapplied; restart the server before concluding an edit did not take.
- (manual) `board.html` carries the change-lifecycle and session controls (Commit, Close change, Reopen, Sessions, New session); they are wired by DOM id in `web/static/app.js`, so keep ids such as `commit-btn`, `close-change-btn`, `reopen-btn`, `sessions-btn`, and `new-session-btn` stable when editing.
- (manual) `layout.html` owns the full-size terminal overlay (`#terminal-overlay` / `.terminal-window` with a single `#terminal-container`) where `app.js` mounts xterm; vendored htmx/Sortable/xterm assets stay at pinned `?v=1` while only `app.css`/`app.js` use the content-hashed `.AssetsV`.
- (manual) `index.html`'s Discussions section is a client-rendered `<ul id="discussions-list">` populated by `app.js`; the only server-submitting form there is the opencode session POST.
- (manual) `explorer.html` defines `content`, the dirs-only `explorerTree`/`explorerNode` fragments, and `explorerDetail`; keep the recursive `{{template "explorerNode" .}}` reference intact or the tree loses its nested directories.
- (manual) Explorer templates read `.Data.Enabled`, `.Root`, `.Dirs`, `.Files`, `.Purpose`, and `.Blurb`, which map to `explorerView`/`explorerNode` built in `internal/server/explorer.go`; a missing purpose renders the "no description yet" placeholder.
- (2026-09-12-8) Each tree `<details>` carries `data-rel` so a tree refresh can restore which nodes were open; the `.explorer-chat` button is no longer in tree summaries (removed in 2026-09-12-11), so clicking a row only selects it.
- (2026-09-12-11) The explorer page is a split shell: `#explorer-tree` holds the dirs-only tree (each `<summary>` carries `hx-get="/explorer/detail?dir={{.Rel}}"` targeting `#explorer-detail`) and `#explorer-detail` starts with root's server-rendered `explorerDetail`; the tree renders no purposes, files, or chat buttons.
- (2026-09-12-9) `layout.html`'s header carries the docs bell (`#notif-bell` plus `#notif-badge`, hidden until findings exist) and the `#notif-modal` containing `#notif-list`, `#docs-refresh-btn`, and `#notif-refresh-status`; its z-index sits below the terminal overlay, and the shared `banner` partial is now rendered only for `changes/` violations.
- (2026-09-12-12) `layout.html`'s header is now `brand | .topnav (Changes `/`, Explorer `/explorer`) | .topnav.topnav-right (Settings `/settings`) | #notif-bell | #theme-toggle`; the active item gets `class="active"` from `{{if}}` comparisons on `.Page` (`index`/`board` → Changes, `explorer` → Explorer, `settings` → Settings), the bell is an inline stroke SVG inside `#notif-bell` with `#notif-badge` kept as-is, and the old `.crumb` span/rule were removed (`.Title` still feeds `<title>`).
- (2026-09-12-12) `board.html` leads `.board-head` with `<button class="btn-accent" id="continue-session-btn">Continue session</button>`; `app.js` fetches the change's sessions on load, flips the label between "Continue session" and "Start session", and reuses the existing `openTerminal`/`POST /changes/{id}/sessions` paths.
- (2026-09-12-14) `layout.html` is branded lessmess in `<title>`; since 2026-09-16-7ueiv its head links the dynamic accent-rendered routes `/icon.svg`, `/favicon.ico`, and `/apple-touch-icon.png` (not the static files), and the header `.brand` and the terminal `.terminal-head` both render the 28px `/icon.svg` (`aria-label="lessmess home"` / decorative) — the `#terminal-session` chip was removed.
- (2026-09-13-0) `index.html` now also carries the `Commit all` button (`#commit-all-btn`, its state from `.Data.GitRepo`/`.Data.GitDirty`) and the `#commit-modal` overlay wired by `app.js`; keep the ids `commit-all-btn`, `commit-modal`, `commit-file-list`, `commit-stat`, `commit-confirm-btn`, and `commit-modal-status` stable.
- (2026-09-13-1) `layout.html` now wraps the terminal in a `.terminal-body` flex row holding `#terminal-container` plus a hidden `<aside id="terminal-tasks">` that `app.js` fills only for board change terminals; keep that id and its `hidden` default, since unassigned terminals must stay full-width.
- (manual) `settings.html` defines `content` for the settings editor and is parsed with `layout.html` into the separate `renderer.settings` set; each savable `.settings-section` carries a `data-section` and one `[data-save]` button (the display-only `general` section added in 2026-09-14-0 has neither), and fields expose `data-field`/`data-kind` plus a `[data-input]` that `initSettings` reads to build the per-section PUT payload.
- (manual) Keep the settings DOM hooks stable because `web/static/app.js` addresses them by id: `#settings-page`, the `input[name="settings-scope"]` radios, `#settings-load-error`, `#settings-options-hint`, `#settings-agent-list`, `#settings-model-list`, and the `.src-badge`/`.settings-status` elements.
- (2026-09-13-2) `settings.html` renders six `.settings-section[data-section]` blocks (general, session, prompts, git, ui, docs; `general` added in 2026-09-14-0) in one `content` template; the `.settings-nav` buttons (`data-group`) select the visible section, and the savable sections' `data-section`/`data-group` values double as the section keys in `lessmess.json` and in `initSettings`' per-section PUT payloads (`general` has no `[data-save]` and never submits) — rename them in template, nav, and `app.js` together.
- (manual) `setup.html` is parsed with `layout.html` into the separate `renderer.setup` set and defines `content` for the five-step wizard (`#setup-page`, `data-step` sections, `data-step-nav` items); `initSetup` in `app.js` drives it, so keep those ids and `data-*` hooks stable, and remember `GET /setup` renders it even on fully initialized repos.
- (2026-09-13-4) `layout.html` renders the setup page chrome-free: `{{if ne .Page "setup"}}` guards both topnavs, the docs bell, and the violations banner, so any new global header element must respect that conditional or it leaks into first-run setup.
- (2026-09-13-4) `setup.html`'s five `data-step` sections are `prereqs`, `bootstrap`, `agent`, `docs`, and `finish`, paired with the matching `data-step-nav` items; their inner controls (`#setup-prereq-list`, `#setup-recheck-btn`, `#setup-exclude-list`, the seed progress and step buttons) are all DOM hooks `initSetup` addresses, so renames must move template and `static/app.js` together.
- (2026-09-13-5) `layout.html`'s `#notif-modal` actions row now holds `#docs-refresh-btn`, `#docs-seed-btn` (whose label gains a pending count from JS), and the `#docs-seed-force` checkbox with its `#docs-seed-force-label` — keep those ids stable, as `app.js` wires them by id.
- (2026-09-13-5) `settings.html`'s docs section gained a `data-field="docs.gardenerModel"` text input bound to the shared `#settings-model-list` datalist, next to the `#docs-exclusions` widget (`#docs-exclusions-save` / `#docs-exclusions-status`); the field-help text documents the session-model fallback when unset.
- (2026-09-14-0) `settings.html` is structured scope strip → split → footer: `.settings-scopebar` holds the `.scope-seg` radiogroup with labels exactly `Personal`/`Project` (the native `input[name="settings-scope"]` radios, values `project`/`personal`, are visually hidden but must stay for `initSettings` and the `render_test.go` `name="settings-scope"` assertion), `.settings-side` holds the `h1` plus `.settings-nav`, and the layer-explanation paragraph (the `lessmess.json` / `.lessmess/settings.json` strings the render test asserts) now lives in `<footer class="settings-foot">` after the split; the old `.settings-head`/`.settings-body` wrappers are gone.
- (2026-09-14-0) The `General` nav button (`data-group="general"`, first item) selects a `data-section="general"` block that only re-links the onboarding wizard via the ghost anchor `<a class="btn-ghost settings-wizard-btn" href="/setup">`; it intentionally has no fields and no `[data-save]`, so keep it that way or `general` would start being submitted as a settings section key.
- (2026-09-15-0) `taskDetail`, `planDetail`, and the new `ledgerDetail` share one detail-modal shape — `.modal-head`, then `.modal-panes` holding an empty `<nav class="modal-toc" hidden>` (app.js populates it, so it must stay present in all three) and `.modal-body.prose` rendered with `{{markdown .Body}}`; `ledgerDetail` displays raw `ledger.md` (`ledgerView`), with its leading H1 dropped server-side via `dropLeadingH1`.
- (2026-09-15-lk9or) `index.html`'s six data `<th>`s now wrap `<button type="button" class="sort-btn" data-sort-col="…">Label<span class="sort-ind" aria-hidden="true"></span></button>` (the trailing Plan `<th>` stays a plain static header), and each body `<tr>` carries `data-tasks`, `data-status-rank` (`{{.Status | statusRank}}`), and `data-updated`; keep those attribute names and the `statusRank` pipe intact — `app.js` sorts on them and `render_test.go` asserts them.
- (2026-09-16-7ueiv) `layout.html`'s `<head>` emits `{{.AccentStyle}}` — the server-rendered inline `<style>` that sets `--accent`/`--accent-hover` for `:root` and `[data-theme="light"]` — from `pageData`, so templates never hardcode palette colors and every page is accent-correct on first paint; the favicon/brand `<link>`s and `.brand-icon` `<img>`s all point at the dynamic routes.
- (2026-09-16-7ueiv) `settings.html`'s `ui` section carries the Accent field (`data-field="ui.accent"`, `data-kind="accent"`) whose `[data-input]` is an initially empty `<div class="accent-swatches" data-value="">` that `app.js` fills with swatch chips — keep that container and the kind hooks intact or `initSettings` skips the field.
- (2026-09-16-le45q) `board.html`/`partials.html` render the drill-down board: the `.crumbs` nav from `.Data.Crumbs`, rollup badges and the `.card-expand` button (`data-expand`) on container cards, and a parent-aware add-task form; `#board` carries `data-task` and modals carry `data-doc` (the task href context `app.js` resolves nested `tasks/...` and `../ledger.md` links against) — keep `#board`, `.cards[data-status]`, `.card[data-task]`, and `data-doc` stable since the terminal panel mirrors them and render tests assert them.
<!-- tasktracker:end -->
