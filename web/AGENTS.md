# web

<!-- tasktracker:begin -->
- Purpose: Go package holding the lessmess web UI's embedded assets — `templates/` (html/template sources) and `static/` (CSS, JS, vendored libraries).
- `web.go` is the whole package: it exposes `FS embed.FS` via `//go:embed templates static`, with no runtime logic of its own.
- Static assets include app CSS/JS plus vendored htmx, Sortable, and xterm libraries; see `static/VENDOR.md` before updating them.
- Gotcha: files must exist at build time; adding a new top-level asset directory or file requires updating the `//go:embed` list in `web.go`.
- Template naming and rendering conventions live in `templates/AGENTS.md`; template names are parsed into one global set.
- (manual) Root `agentsdocs.json` excludes only `web/static`, so JS/CSS there is outside docs coverage while `templates/` remains covered by the doc gardener.
- (manual) Because `//go:embed templates static` is a directory embed, new template and static files are picked up automatically as long as they live under those trees; `explorer.html` is parsed into the same global template set as the rest.
- (2026-09-12-8) The explorer's client behavior lives in `static/app.js` (a `docs` SSE listener that refreshes `#explorer-tree` and restores open nodes, plus a delegated `.explorer-chat` click that opens the created session in the terminal overlay) and its tree styling in `static/app.css`; both are under docs-excluded `web/static`.
- (2026-09-12-11) The explorer is now master/detail: `static/app.js` tracks the selected directory, restores open nodes plus the selection after a tree swap, and re-fetches `#explorer-detail`; tree fragments injected via `innerHTML` must be passed to `htmx.process` so the new `hx-get` summaries are wired, and the chat button moved to the detail header so its propagation guard runs in the capture phase.
- (2026-09-12-9) The docs notification bell/badge and findings modal are client-rendered in `static/app.js`: `checkValidation` caches docs findings (the banner is now violations-only), the badge re-renders on each check and on the `tt:docs-event` custom event, and the modal's refresh button posts `/docs/refresh` with a busy/result state that re-enables on the next docs SSE event, with styling in `static/app.css` (both under docs-excluded `web/static`).
- (2026-09-12-12) The shared header is now a `Changes`/`Explorer` topnav whose `.active` orange text+underline is applied server-side from `pageData.Page`, and the docs bell is a flat inline `currentColor` SVG (both `static/app.css`); the board's one-click resume tracks the last-opened session per change in `localStorage` as `tt-last-session:<id>` and falls back to the newest created entry (`static/app.js`). All under docs-excluded `web/static`.
- (2026-09-12-14) The UI is branded lessmess and carries a new identity: `static/icon.svg` (white "lm" on the `#e8641f` accent) plus generated `icon-512.png`, `favicon.ico` (16/32/48), and `apple-touch-icon.png`, all under docs-excluded `web/static` and linked from `layout.html`.
- (2026-09-12-15) The `Test` status pill uses `--st-test-bg`/`--st-test-fg` in both the dark and light blocks of `static/app.css` plus a `.status-test` rule; the `statusClass` helper already maps `Test` to `status-test`, and the close-change confirm in `static/app.js` intentionally still warns while `Test` tasks remain unpromoted.
- (2026-09-13-0) The index page's Commit all flow is client-side in docs-excluded `static/app.js` (`initCommitAll`): `#commit-all-btn` fetches `/api/git/status`, renders one merged porcelain row per file with per-file `+/-` counts plus a summary line, then on confirm posts `/api/git/commit` and polls `/api/git/commit-status` until "✓ Committed", when the button disables and `loadDiscussions()` surfaces the session; its modal styles live in `static/app.css`.
- (2026-09-13-1) The embedded-terminal task panel is client-side in docs-excluded `static/app.js` (`syncTerminalTasks`): it mirrors the board DOM (`.cards[data-status]` columns and `.card[data-task]`/`.card-title[hx-get]` rows) into `#terminal-tasks`, is unhidden only for board change terminals in `openTerminal`, and re-syncs from the existing `#board` `htmx:afterSwap` hook so live updates need no new server surface.
- (2026-09-13-1) Panel row clicks reuse each card's own `hx-get`/`hx-headers` targeting `#detail`, and `.ttp-*` styling lives in docs-excluded `static/app.css`; because `#detail` now stacks above the terminal overlay, the global Escape handler closes a visible `#detail` before the terminal.
- (manual) The settings editor is client-rendered by `initSettings` in docs-excluded `static/app.js`: it loads `/api/settings` and `/api/settings/options`, toggles the project/personal scope, fills per-field source badges, and saves one section at a time via `PUT /api/settings?scope=...`; the same effective `session.autoOpenTerminal` is fetched once per page load to gate terminal auto-open.
- (manual) `settings.html` is parsed with `layout.html` into its own `renderer.settings` template set (separate from the board/index sets), so it must keep defining the shared `content` template and its `data-field`/`data-kind`/`data-input` and section `[data-save]` hooks that `app.js` reads.
- (2026-09-13-2) The agent and model inputs are `<datalist>` comboboxes (`#settings-agent-list` / `#settings-model-list`) rather than `<select>`s: `initSettings` fills them from `/api/settings/options` (empty when the opencode service is offline) while the inputs stay free text, so settings remain editable and savable offline.
- (manual) The setup wizard's client logic is `initSetup` in docs-excluded `static/app.js` (step nav, prereq re-check, bootstrap, agent/model datalists, docs-seed polling for `setup.html`), and the index page's finish-setup banner dismiss is `initOnboardingBanner` posting `/api/setup/dismiss`; `setup.html` is parsed into its own `renderer.setup` set, not the board/index one.
- (2026-09-13-4) `initSetup` flow specifics: the prereq step gates Continue on `ready` (only `fail` checks block), bootstrap POSTs `{docsCoverage, excludeDirs}` and re-submitting it later also updates an existing `agentsdocs.json`, the agent/model step defaults to the personal scope with a project radio and can be skipped, the docs step is opt-in with a cost warning and polls `/api/setup/docs-seed-status` for live progress, and Finish POSTs `/api/setup/complete` before redirecting to `/`.
- (2026-09-13-4) The exclusion picker is a lazy expandable tree fed by `GET /api/setup/dirs?dir=...` — top-level picks write base-name patterns that prune whole subtrees, nested picks write exact paths — and the Settings page's `#docs-exclusions` editor reuses the same entries and saves via `POST /docs/exclusions`; all wizard styling (`.setup-*`, centered card) lives in docs-excluded `static/app.css` since the setup page renders without app chrome.
- (2026-09-13-5) The docs bell modal gained a seed action: `#docs-seed-btn` relabels to "Run missing docs (N)" from the `docsSeedPending` count in `/api/validate` and posts `/docs/seed` with `{force}` from the `#docs-seed-force` checkbox (a confirm guards force, which redoes every covered dir); like the refresh button it disables while running and re-enables on the next docs SSE event.
- (2026-09-13-5) The Settings docs section adds `docs.gardenerModel` (string field, `data-field="docs.gardenerModel"`): `initSettings` shows an "inherits session model" badge when unset, fills it from the shared `#settings-model-list` datalist, and maps its source badge to `session.model` while inheriting — it saves like any other docs-section field.
- (2026-09-14-0) The top menu bar is sticky on every page: `static/app.css` gives `header` `position: sticky; top: 0; z-index: 20` with a fixed 52px height, under the ladder header 20 < modals 25 < terminal overlay 30 < board detail panel 40 — so the header is hidden only while the overlay or detail panel is open; keep new fixed or sticky surfaces inside that ladder.
- (2026-09-14-0) The settings page is a full-height app shell styled in docs-excluded `static/app.css`: `body[data-page="settings"]` is a 100vh flex column with `overflow: hidden` (board pattern), the full-bleed `.settings-scopebar` is sticky at `top: 52px` z-index 19 holding the segmented `.scope-seg` Personal/Project control over the untouched native `input[name="settings-scope"]` radios, `.settings-split` puts the 13.5rem `.settings-side` (h1 + `.settings-nav`) beside an internally scrolling `.settings-content`, and `.settings-foot` is pinned flush to the viewport bottom; below 640px the media query restores page scroll and stacks the panes.
- (2026-09-14-0) The whole settings restructure needed zero `static/app.js` edits because `initSettings` still finds every hook (`#settings-page`, the scope radios, `.settings-nav` + `data-group`, `data-field`/`data-kind`/`[data-input]`, `[data-save]`, badges, datalists) — reshape `settings.html`/`app.css` freely but keep those hooks, and keep the display-only `general` section free of fields and `[data-save]` so it never reaches `PUT /api/settings`.
- (2026-09-14-1) The index page's SSE handler (`scheduleRefresh` in docs-excluded `static/app.js`) no longer full-reloads while a terminal is open with a live session: `followSession()` polls `/api/sessions/{id}/change` and, once that discussion scaffolds into a change, navigates to `/changes/{id}?session=` so `autoOpenSession` reconnects the same terminal and task panel on the new board. Never reintroduce an unconditional index `location.reload()` — it destroys the overlay; the scaffold writing `changes/` is exactly what fires the event.
- (2026-09-14-1) Follow-up semantics to keep: while the terminal is open on the index, background `changes/` writes leave the change list stale by design (boards are immune — they refresh via htmx fragment swaps), and `closeTerminal()` fires one deferred `scheduleRefresh()` so skipped refreshes catch up once a reload is safe again.
- (2026-09-15-0) The detail modal gained in-modal navigation, entirely client-side in docs-excluded `static/app.js` (+ `static/app.css`): on every `#detail` `htmx:afterSwap`, `buildDetailTOC()` fills the `.modal-toc` rail from h2/h3 ids (hidden under 2 headings) and toggles `has-toc` on `.modal`, which widens it to `min(880px, 100%)`; click-to-scroll and an rAF-throttled scroll-spy run on `.modal-body`.
- (2026-09-15-0) A delegated click handler on `#detail` keeps relative `.md` links in-modal by mapping them to detail endpoints — `tasks/<file>.md`, `plan.md`, `(../)ledger.md`, and cross-change `YYYY-MM-DD-N/plan.md` — fetching with `Accept: text/html` and swapping `#detail.innerHTML` (close handlers survive because they are delegated too); absolute URLs and non-`.md` links are untouched.
- (2026-09-15-lk9or) The index table is sortable client-side in docs-excluded `static/app.js` (`initIndexSort`): header clicks sort `<tbody>` rows from row attributes (`data-tasks`/`data-status-rank` numeric, `data-updated` string, cell text for Change/Title/Prefix), toggle the `sort-asc`/`sort-desc` indicator plus `aria-sort` on the active `<th>`, and persist `{col, dir}` to localStorage `tt-index-sort`, re-applied on every load so the choice survives `scheduleRefresh()`'s full reload; corrupt or unknown stored values fail open to the server's newest-first default. Styling in `static/app.css`.
<!-- tasktracker:end -->
